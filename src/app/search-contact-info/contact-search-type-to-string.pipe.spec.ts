import { ContactSearchTypeToStringPipe } from './contact-search-type-to-string.pipe';

describe('ContactSearchTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactSearchTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
